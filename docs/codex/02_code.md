# Codex review — 02_code

model: gpt-6-astra · effort: ultra · sandbox: read-only · 2026-09-15T17:14:47+06:00

CODE-BLOCKERS: 9

1. **[BLOCKER] `scoping/kernel_map.py:74` — Kernel metadata can silently disappear.** The parser requires `Original ATen:` within eight lines before a descriptive Triton `def`; it does not reliably associate metadata with the wrapper’s `name = async_compile.triton(...)` assignment. Longer preludes or generic inner names defeat that association. Missing ops become `UNKNOWN`, and unmatched trace kernels receive zero duration, yet a headline ratio is emitted. **Minimal fix:** associate metadata with each enclosing compile assignment and wrapper symbol, retain graph/file/line identity, and require coverage of relevant kernels. Unresolved mapping must propagate to the specified lower-bound status.

2. **[BLOCKER] `scoping/kernel_map.py:107` — Input identification, permutation detection, and byte accounting are incorrect.** Reinterpret arguments are collected first, changing argument order; consequently `args[:-1]` cannot reliably exclude outputs. Argument records also discard allocation dtype, causing `bytes_of()` to assume fp16. Finally, the study’s own permuted shape/stride `(60,801,384)/(384,23040,1)` returns `False` from `is_permuted()` because its last stride remains one. **Minimal fix:** preserve argument order and dtype, resolve aliases, derive actual read/write roles, and inspect full stride order plus upstream permutations. Reuse this logic in `inventory.py`, which currently counts outputs as possible reads.

3. **[BLOCKER] `scoping/microbench_p_l2.py:101` — BW_ref does not measure the required corresponding kernel class.** Every B site receives the median bandwidth of copy, normalization, and GELU. Those measurements include host/launch/synchronization overhead, whereas the numerator uses GPU kernel durations. The standalone chains also do not establish matching autocast behavior or output layout: `clone()` can preserve the permuted input’s strides, avoiding the conversion being investigated. **Minimal fix:** reproduce each representative mapped chain’s dtype, operations, and required output layout; verify its generated accesses; measure GPU execution duration; and select its class-specific contiguous BW_ref.

4. **[BLOCKER] `scoping/kernel_map.py:243` — A/B include work outside the transformer stack.** Everything before the first SDPA is assigned to the first sublayer; everything after the final SDPA is assigned to its `_post` segment. COPY extras additionally include every COPY-class launch in the representative window. Band splitting, reconstruction, casts, and padding can therefore enter the layout numerator without axial-layout provenance. **Minimal fix:** establish transformer boundaries and actual layout-change provenance, map external COPY launches into those boundaries, and average their contributions over complete passes. Triton/COPY classifier precedence itself prevents double counting between those two classes.

5. **[BLOCKER] `scoping/kernel_map.py:192` — Pass and call-site attribution are accepted without validation.** Pass count comes from classified SDPA launches divided by parsed wrapper anchors; `iters_completed` is merely printed. Missing or duplicated anchors can corrupt the divisor. Largest-gap cuts can corrupt the representative pass and COPY extras even when the divisor happens to remain correct. Sorted dump paths, prefix matching, and equal per-name sharing also do not establish runtime call-site correspondence. **Minimal fix:** align executed graphs and launches using outer-pass NVTX/correlation evidence, require 50 complete passes with 12 alternating known-axis anchors, and aggregate matched call-site durations. Equal sharing is acceptable only after equivalent sites are established.

6. **[MAJOR] `scoping/kernel_map.py:263` — The heads-views verdict does not examine producers or head merging.** A reinterpretation of a newly materialized buffer passes this test. Conversely, a directly supplied rotary output can fail it. No SDPA-output-to-projection head merge is inspected. The inventory also omits fused materializations, producer/output stride changes, and consumers required by adopted update 2. **Minimal fix:** trace q/k/v allocation ancestry, distinguish rotary outputs from additional layout copies, inspect head merging separately, and emit materializations with sizes, consumers, and actual output-code file/line references. Use `UNRESOLVED` where ancestry is unavailable.

7. **[MAJOR] `scoping/kernel_map.py:335` — The CSV cannot reproduce the published numerator.** External COPY contributions appear only in JSON, B contributions are textual values rounded to 0.1 μs, and the per-layer table excludes COPY extras. SDPA-to-SDPA segments also combine one sublayer’s post-attention work with the next sublayer’s preparation. **Minimal fix:** emit a complete ledger with numeric, unrounded A/B contributions, verified ownership, pass normalization, and denominator provenance. Derive JSON totals and per-layer sums from that ledger.

8. **[BLOCKER] `scoping/common_timing.py:45` — Failed settlement does not stop measurement.** A capped run returns normally, and every driver continues into validation and timing without achieving five stable passes. **Minimal fix:** persist the settle record and abort dependent measurement when `settle_capped` is true; prevent finalization from accepting that run.

9. **[BLOCKER] `scoping/common_timing.py:116` — Validation can return a false PASS.** `torch.allclose` permits broadcasting and matching infinities. Thus a broadcast-compatible wrong shape or matching infinite outputs can pass; the latter can also produce `max_abs_diff=NaN`. **Minimal fix:** require exact shape equality and finite values in both outputs before applying tolerance checks, and serialize an explicit failure reason.

10. **[BLOCKER] `scoping/knob_run.py:81` — Knobs use the wrong acceptance reference and tier.** They validate loose against eager outputs. Binding handoff §2 requires Phase-2 interventions to validate tight against the compiled baseline, so this cannot supply the acceptance verdict for `flag_recovers`. **Minimal fix:** load `ref_baseline_<seed>.pt` and compare at the tight tier; retain loose/eager results only as supplementary diagnostics.

11. **[BLOCKER] `scoping/finalize.py:50` — R_recovered is published without the required acceptance checks.** Finalization never consumes patched materialization/copy-verification evidence. It also retains a numeric `R_recovered` after tight validation fails, merely adding a status field. A patch with dispatch copies or failed equivalence can therefore receive a headline recovery number. **Minimal fix:** require successful settlement, tight validation, reduced materializations, and the required adjacent-copy inspection. Otherwise emit `FAILED(reason)` with no numeric headline recovery; keep any diagnostic calculation in a separate field.

12. **[MAJOR] `scripts/run_phase2.sh:35` — The knob workflow never completes flag_recovers.** Changed inventories never trigger the promised full trace, and `finalize.py` emits no `flag_recovers` value. Equality of three aggregate inventory totals cannot establish unchanged A+B. The sweep also omits `triton.persistent_reductions=False`, promised by the design notes and P-L4. **Minimal fix:** add that knob, implement follow-up attribution, and emit the ≥60%-reduction verdict with tight validation. Where measurements cannot establish a verdict, emit `UNTESTED(reason)`.

13. **[MAJOR] `scripts/beast_env.sh:31` — Clock snapshots cannot satisfy the clock audit.** Pre/post samples do not establish sustained clocks during timed blocks or baseline/patched agreement within 5%. **Minimal fix:** log GPU 4 SM/memory clocks and power every second in the background, record timed-block timestamps, and assess comparability before accepting recovery.

14. **[MAJOR] `scripts/run_phase2.sh:39` — Resume can reuse stale or incomplete evidence.** Any existing knob JSON causes a skip, including FAILED/UNTESTED results and interrupted runs whose inventory was never generated. No fingerprint binds reused artifacts to the current baseline, code, weights, config, or environment. **Minimal fix:** reuse only artifacts matching a run manifest and completeness check; regenerate missing postprocessing and explicitly handle failed/untested runs. Apply the same provenance check to phase prerequisites.

15. **[MAJOR] `scripts/run_phase2.sh:28` — Skipped/failed paths and final deliverables are incomplete.** The threshold skip exits before finalization. No inlined code creates `RESULT.md`, stages artifacts into `evidence/`, or assembles the required hashes and evidence links. `summary.json` also omits several required reporting fields, including throughput, spreads, and baseline per-seed validation. **Minimal fix:** add a common reporting/staging path for successful, skipped, and failed outcomes, preserving explicit unresolved reasons and all required provenance.

16. **[MAJOR] `scripts/beast_env.sh:46` — Required projection-export failure is reported as success.** Both NVTX export attempts may fail, after which `nsys_export` returns zero and the phase declares completion. The summary report also does not provide the launch boundaries needed for reliable pass correlation. **Minimal fix:** require the specified projection summary and export the projection trace when needed for alignment; fail or explicitly mark the dependent attribution incomplete when unavailable.

17. **[MINOR] `scoping/knob_run.py:94` — Screening and trace medians use the upper middle observation.** For 50 samples, `s[len(s)//2]` is not the median used by `five_repeats`; both trace drivers repeat this discrepancy. **Minimal fix:** use `statistics.median` consistently.

18. **[NOTE] `scoping/axial_patch.py:1` — Upstream/compile parity remains unverified from this packet.** The supplied stride, rotary-axis, gate, residual, FF, and final-norm algebra is internally consistent; patching precedes compilation and batch one matches the study. Stock helpers and generated code are absent, so norm promotion, imports/einops capture, and native-output view claims need evidence. **Minimal fix:** supply pinned-source/diff and compile evidence; correct the “loss branches included” claim to inference-only.

run_phase0 → Wires the stated C1 call, seeds, loose validation, and five complete 5a repeats; settlement must fail closed.
run_phase1 → Opens one capture and uses consistent export filenames; trustworthy attribution and complete audit evidence remain blocked.
run_phase2 → Wires patch validation/timing, but verified recovery, changed-knob traces, and flag_recovers are incomplete.
Skip/failure paths → Do not produce the required final report and staged evidence; add common finalization.
Overall → No: these scripts do not yet produce every RESULT.md artifact or make all seven owner-audit checks executable.
---
exit: 0
